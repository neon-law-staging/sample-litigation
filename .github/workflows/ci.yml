name: ci

on:
  pull_request:

jobs:
  ci:
    uses: neon-law-source-code/navigator/.github/workflows/project-gate.yml@26.9.15-rc.2
    with:
      project: "sample-litigation"
      host: "staging.neonlaw.com"
